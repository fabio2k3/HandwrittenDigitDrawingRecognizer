# HandwrittenDigitDrawingRecognizer
A PyTorch-based AI application that allows users to draw handwritten digits (0–9) in a graphical interface and predicts the drawn number using a convolutional neural network trained on MNIST.
